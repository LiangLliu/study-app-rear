server:
  port: 8080
  servlet:
    context-path: /study

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://39.105.71.174:3306/study?characterEncoding=utf-8&useSSL=false
    username: study
    password: eMr6ahENTK5zE2wA
  jpa:
    hibernate:
      ddl-auto: update # 如果数据库不存在就创建
    show-sql: true    # 打印数据库语句

#  jackson:
#    default-property-inclusion: non_null  # 如果为空，不返回

mybatis:
  mapper-locations: classpath:/mapper/*Dao.xml